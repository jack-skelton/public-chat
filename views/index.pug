extends layout

block content
  form(method="post")
    input(type='text' name='name' placeholder='Name')
    input(type='text' name='message' placeholder='Please leave a message...')
    input(type='submit' value='submit')
  ul
    each message, index in messages
      p= message.name
      p= message.message
      p= message.time
